sync:
  toHost:
    ingresses:
      enabled: true
    persistentVolumeClaims:
      enabled: true
    services:
      enabled: true
  fromHost:
    nodes:
      enabled: true

exportKubeConfig:
  context: vcluster-app-a
